# Example only — DO NOT put real values here or commit a real Secret.
# Create the real Secret imperatively instead:
#
#   kubectl create secret generic shortlink-secret \
#     --namespace shortlink \
#     --from-literal=POSTGRES_PASSWORD=<password> \
#     --from-literal=DATABASE_URL=postgresql+psycopg://postgres:<password>@postgres:5432/shortlink
#
# This file documents the Secret's shape for anyone setting the project up.
apiVersion: v1
kind: Secret
metadata:
  name: shortlink-secret
  namespace: shortlink
type: Opaque
stringData:
  POSTGRES_PASSWORD: changeme
  DATABASE_URL: postgresql+psycopg://postgres:changeme@postgres:5432/shortlink
