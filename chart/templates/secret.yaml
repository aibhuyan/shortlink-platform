{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: shortlink-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.postgres.password | quote }}
  DATABASE_URL: {{ printf "postgresql+psycopg://%s:%s@%s:5432/%s" .Values.postgres.user .Values.postgres.password .Values.postgres.host .Values.postgres.db | quote }}
{{- end }}
